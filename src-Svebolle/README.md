## NETWORK TOPOLOGY

Check the network-topolgy-model directory for topology overview; details are not accessible.

## DATA TRACES INFO

* Measured parameters: timestamp, device ID, MAC payload, RSSI, SNR, SF, CR, message type...

* 1 gateway / access point / base station

* few hundred end devices


